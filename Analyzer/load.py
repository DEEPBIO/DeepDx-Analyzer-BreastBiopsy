import os
from model import load_model
import torch
import torch.backends.cudnn as cudnn

def load_breast_biopsy_model(checkpoint_path) -> torch.nn:
    ''' load breast resection model into GPU memory '''

    return None
